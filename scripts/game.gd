# script game.gd

extends Node

const GROUP_CAR = "car"
const GROUP_ENEMY = "enemy"
const GROUP_BLOCKS = "blocks"

func _ready():
	# Called every time the node is added to the scene.
	# Initialization here
	pass
